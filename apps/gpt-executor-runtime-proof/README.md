# GPT Executor Runtime Proof Fixture

This workspace is an arbitrary standalone project fixture used to validate that the ACP GPT Executor runtime operates strictly within resolved workspace boundaries.
